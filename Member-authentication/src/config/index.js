const database = require("./database");
const passport = require("./passport");
const seed = require("./seed");
// Import the database.js and passport.js files

// Export the modules
module.exports = {
  database,
  passport,
  seed,
};
